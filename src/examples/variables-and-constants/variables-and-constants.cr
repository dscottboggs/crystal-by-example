# To store a value for later, simply assign it to a name with the equals
# operator
some_var = "a value"
# You can specify the type of a value after a space-separated colon and type
# name before the assignment operator
another : Float32 = 3
# If the name of a value starts with a capital letter, that name represents a
# constant which can't be reassigned
CONST_VALUE = 123.456
# By convention, all variables are in lower_snake_case, constants are in
# UPPER_SNAKE_CASE, while type names use PascalCase
